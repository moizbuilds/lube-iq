# Lube IQ

Find the right engine oil for your machine, compare oils spec-by-spec, and
discover cross-brand equivalents. Static React SPA — Vite + React, no backend.

- `npm install` then `npm run dev` — dev server on http://localhost:5173
- `npm test` — unit tests (Vitest)
